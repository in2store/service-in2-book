name: service-in2-book
group: in2store
version: 0.0.0
description: 书籍管理
program_language: golang
workflow:
  extends: feature-pr
scripts:
  build: go build -v -o $PROJECT_NAME && libtools swagger
  test: go test
